function displayData(jsonData) {
    var attributes = Object.keys(jsonData[0]);
    console.log("Attributes in each record: " + attributes.join(", "));

    // Rest of the code to display the data in a table
    // ...
}